import assert from 'assert';
// const assert = require('assert');

assert(true); // assert.ok() alias.

/* evaluated to falsy value: */
// assert('')
// assert(0);
// assert(false);
// assert(null);
// assert(undefined);
